# Customer-Segmentation
Implemented K-Means clustering on a customer segmentation dataset. Preprocessed mixed data types (numerical/categorical) and validated unsupervised clusters against ground-truth labels using a confusion matrix heatmap, identifying distinct customer profiles.
